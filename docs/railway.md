# Railway

The application is deployed in production on [Railway](https://railway.com/dashboard). The application is setup to deploy automatically whenever new commits are pushed to the `main` branch.

Railway will build the application using the `Dockerfile` in the repository.
