# Redis

The application uses Redis with [Redis-OM](https://github.com/redis/redis-om-python) for database.

## Setup

A free redis instance can be spun up with [Redis Cloud](https://cloud.redis.io/). Copy the connection url to the environment variable `REDIS_OM_URL`.
